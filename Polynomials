import numpy

# Read the coefficients of the polynomial
coefficients = list(map(float, input().split()))

# Read the value of x
x = float(input())

# Evaluate and print the polynomial at point x
print(numpy.polyval(coefficients, x))
